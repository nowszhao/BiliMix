# ============================================================
# BiliMix 主项目依赖
# 一次性安装：./setup.sh  或  pip install -r requirements.txt
# TTS 依赖见 requirements-tts.txt（由 setup.sh 自动安装）
# ============================================================

# ---- Web 服务 ----
flask>=2.3
requests>=2.28

# ---- 音频处理 ----
pydub>=0.25
soundfile>=0.12

# ---- 视频下载（yt-dlp 命令行，用 subprocess 调用）----
yt-dlp>=2024.0

# ---- 人声/背景分离（通过 `python -m demucs` 调用，必须装在主 Python 环境）----
demucs>=4.0

# ---- Whisper 转录 ----
# WhisperX 通过 subprocess 调用 whisperx 二进制，不是 import
# 但仍然需要 torch / torchaudio 用于 demucs 和 TTS
torch>=2.0
torchaudio>=2.0

# ---- ffmpeg（用 subprocess 调用 ffmpeg 命令行）----
# 系统依赖，需用 brew/apt 安装：brew install ffmpeg
# Python 侧的 ffmpeg-python 仅用于部分 API，装上更稳：
ffmpeg-python>=0.2


# ============================================================
# Confucius4-TTS-CPU 依赖见 requirements-tts.txt
# setup.sh 会自动：
#   1. pip install -r requirements-tts.txt
#   2. git clone https://github.com/nowszhao/Confucius4-TTS-CPU.git ../Confucius4-TTS-CPU
# Confucius4-TTS 本体不 pip install，运行时通过 sys.path 从源码树加载
# ============================================================
